export enum StatusEnum {
    Draft = "draft",
    InProgress = "inProgress",
    Paused = "paused",
    Done = "done",
    Failed = "failed",
}

export const ACTION_TYPE = {
    CREATED: "created",
    DELETED: "deleted",
    UPLOADED: "uploaded",
    RAN: "ran",
};
