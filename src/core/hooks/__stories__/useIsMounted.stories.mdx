import { Meta, Story, Canvas } from "@storybook/addon-docs/blocks";

<Meta title="Design System/Core/Hooks/useIsMounted" />

## useIsMounted

```js
import { useIsMounted } from "@ds-tools/design-system/core/hooks";
```

track if component is already mounted (no re-ender).

```js
function useIsMounted() => boolean;
```
